
start: def test(): void{
integer k;

init(integer i << 1, j << 1, k<<1)
       do {
        ("ciao, ") -->;
        (i)-->;
        (", ") -->;
        (j)-->!;
        (k)-->;
        }
       for(i < 20 and j >-20)
       step(i << i+1 ;j << j-1; k<<i+1);

}